Webform component entity module.
1. Installation
 + install this module and dependcy modules
 + import view from view.txt
 
2. In configure of this module
 + input the path of view (ex: component-library) and save (in case of using differnce view)
 Note: if using differnce view need configurate additional fields:
 FIELDS: add Bulk operations: Content
    in 'Selected bulk operations' -> check to 'Add existing component to webform'
    
    
NOTE:
This code not include:
1. Simple test
2. Coder